/**
 * @file The implementation of the various marital statuses that a User can have.
 */

/**
 * Enum for User marital statuses
 * @enum {string}
 */

enum MaritalStatus {
    Married = 'MARRIED',
    Single = 'SINGLE',
    Widowed = 'WIDOWED'
}

export default MaritalStatus;
