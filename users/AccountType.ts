/**
 * @file The implementation of the various User account types available to a User
 */

/**
 * Enum for User account types
 * @enum {string}
 */
enum AccountType {
    Personal = 'PERSONAL',
    Academic = 'ACADEMIC',
    Professional = 'PROFESSIONAL'
}

export default AccountType;
